// Name the date variable with current_Date and pass it to a date method

const Current_Date = new Date();

//Console.log the date Current_Date to see the current date on the console || terminal
console.log(`Current date and time: ${Current_Date}`);
